fastapi>=0.110
uvicorn[standard]>=0.29
# Also the Gemini transport: graph/llm.py calls the REST API directly so the
# agent works with both AIza and the newer AQ. Google key formats, which
# client libraries don't agree on yet.
httpx>=0.27
pyyaml>=6.0
langgraph>=1.0
langgraph-checkpoint-postgres>=2.0
psycopg[binary]>=3.1
pytest>=8.0
